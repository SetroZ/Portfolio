interface blogType {
  id: number
  title: string
  body: string
  image: string
  likes: number
}

const BlogCard = ({ id, title, body, image, likes }: blogType) => {
  ;<div className=''></div>
}

export default BlogCard
